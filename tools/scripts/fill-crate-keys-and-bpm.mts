/** Deprecated: ordinary catalog analysis now owns key storage and selection. */
throw new Error(
  "This maintenance script is retired. Run pnpm cli analysis:run --scope stale --wait, or pnpm cli mix:create --brief-json brief.json --wait.",
);
export {};
